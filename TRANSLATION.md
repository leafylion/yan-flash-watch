# 妖精乱舞 攻略 한글 번역 (docs/index.html)

`yan-flash.com/ultimate/yosei-ranbu` 공략을 한국어로 번역한 결과물입니다.
원문이 업데이트되면 **완전 자동**으로 최신화됩니다 (GitHub Actions `watch.yml`):

1. `check.py` — 更新履歴 감시 → 변경 시 Discord 알림
2. `tools/fetch_guide.py` — 攻略 본문을 페이즈별로 스냅샷(`state/guide/`)
3. `tools/translate.py` — `state/guide/`에 diff가 있으면 Anthropic API로 `docs/index.html` 최신화
4. `state/`·`docs/` 변경을 commit & push → GitHub Pages 자동 재배포

> 자동(구글) 번역이 아니라, 아래 **용어집을 시스템 프롬프트로 강제**해 로컬라이즈 품질을
> 유지합니다. 단 사람 검수가 없으므로 가끔 오역/구조 어긋남이 생길 수 있습니다.

**필요한 GitHub Secret:** `ANTHROPIC_API_KEY` (없으면 번역은 건너뛰고 스냅샷만 갱신),
`DISCORD_WEBHOOK`. 워처는 `WATCH_UNTIL`(기본 2026-06-20) 이후 자동 정지.

## 수동 갱신 절차 (API 없이 손으로 할 때)

```bash
# 1) 최신 본문 스냅샷 갱신
python3 tools/fetch_guide.py

# 2) 무엇이 바뀌었는지 확인 (이미지 교체 / 문구 수정 / 섹션 추가·삭제)
git diff state/guide/

# 3) 바뀐 부분만 docs/index.html 에 반영 (아래 용어집 준수)
#    - 이미지 URL: https://yan-flash.com + /api/uploads/... 그대로
#    - 좌측 목차(nav.toc)에 섹션 추가/삭제 시 링크도 같이 수정

# 4) 커밋 & 푸시
git add state/guide docs/index.html
git commit -m "update: 妖精乱舞 攻略 한글 번역 동기화"
git push
```

## 로컬라이징 용어집 (일관성 유지용)

| 원문(일본어) | 번역 | 비고 |
|---|---|---|
| 頭割り | 쉐어 / 쉐어로 처리 | 표식·디버프는 "쉐어", 모이는 동작은 "쉐어로 처리/쉐어 처리 후" |
| 散会 | 산개 | |
| タンク強攻撃 (탱 대상) | 탱버스터 | 거리 기준 등 탱 비대상 強攻撃은 "강공격" 유지 |
| 全体攻撃 | 전체 공격 | |
| 半面AoE / 半面焼き | 반갈 / 반갈 장판 | |
| 真偽 (本物/偽物) | 진짜/가짜 | "진위" 아님 |
| ヘイト | 어그로 | |
| タンヒラ | 탱힐 | |
| ノックバック / 吹き飛ばし | 넉백 | |
| 詠唱 | 시전 | |
| フェーズ移行 | 페이즈 전환 | "이행" 아님 |
| 着弾 | 문맥별: "장판이 터진 뒤" 등 | 데미지 발동/장판 폭발 등 상황에 맞게 |
| 野良 | 막공 | |
| 集合 | 집합/모이기 | |
| 塔 / ビーム / 線 | 타워 / 빔 / 선 | |
| 扇 / 円 | 부채꼴 / 원형 | |
| グラビガ / 重力弾 | 그라비가 / 중력탄 | |
| 堅実魔 / アームズレングス | 확정 시전 / 암즈렝스 | 넉백 방지 스킬 |

스킬 고유명(바리바리 루인가, 즈비즈바 텔레포, 소멸의 다리 등)은 음차/직역 +
옆에 원문 일본어를 `<span class="jp">`로 작게 병기.

## 이미지 처리 (이미지별 캡션)

이미지(배치도)는 픽셀 편집하지 않고 **원본 그대로** 두되, 각 이미지/이미지 그룹 바로 아래에
`<div class="cap">` **캡션**을 붙인다.
- 이미지 안에 일본어 문장/라벨이 있으면 그 내용을 한국어로 번역해 캡션에 적는다(특히 P3 디버프 설명).
- 아이콘·숫자·방위뿐이면 표기 안내만 간단히(직업 아이콘=담당자, 숫자=순번, A·B·C·1~4=방위 등).

자동 파이프라인에서는 `translate.py`가 diff에서 **추가/교체된 이미지 URL**을 뽑아 Anthropic API에
(URL 이미지 소스로) 첨부하므로, 모델이 새 이미지를 직접 보고 해당 캡션을 작성/갱신한다.

## 제외 규칙

- 원문에서 **삭제 예정**으로 명시된 처리법(예: 旧散会, ②ミッシング ぴ)은 번역본에서 제외.
- 단, 신규 추가된 "최신" 처리법은 포함.

## 파일 구성

- `docs/index.html` — 번역 결과물 (GitHub Pages 루트)
- `tools/fetch_guide.py` — 원문 본문을 페이즈별로 디코드해 스냅샷
- `state/guide/phaseN.html` — 원문 본문 스냅샷 (diff용)
- `state/latest.json` — `check.py`의 更新履歴 스냅샷 (변경 감지·Discord 알림용)
